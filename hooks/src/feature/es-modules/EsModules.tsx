import { useEffect } from "react";
console.log(import('./Modules').then(module => console.log(module.hello)));

export default function EsModules()
{

    useEffect(() =>
    {
    }, []);
    return (
        <div>

        </div>
    );
}
